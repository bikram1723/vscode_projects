document.addEventListener('DOMContentLoaded', function() {
    const button = document.getElementById('myProducts');

    button.addEventListener('click', function() {
        alert('You clicked the button');
    });
});